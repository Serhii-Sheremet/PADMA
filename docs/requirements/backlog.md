# PADMA Backlog

## 1. Purpose

This file contains only deferred or future-facing work. It is not a replacement for the detailed
requirements in the thematic documentation files.

## 2. Platform and Infrastructure

- Complete iOS native Swiss Ephemeris integration when the iOS build becomes active.
- Continue monitoring platform-specific time-zone behavior, especially for historical and future dates.

## 3. Calendar and Day Views

- Add additional DayOverview summary text if requested by the Product Owner.
- Consider configurable timeline resolution only if the current fixed timeline becomes insufficient.
- Add additional day markers or icon types only when tied to a confirmed feature.

## 4. User Events and Reminders

- Per-note reminder overrides remain outside the current notification stage.
- Richer event analytics or additional event interactions may be added later if requested.

## 5. Transit Charts and Rectification

- Continue the Birth Time Rectification Preview in staged steps after the initial workflow is confirmed.
- Add further chart controls only after the Product Owner validates the current chart behavior.

## 6. Monthly Planet Transits

- Add visible Tithi display to the Monthly Planet Transits page if confirmed by the Product Owner.
- Introduce a reusable `MonthlyPanchangaPack` only when Panchanga is displayed directly on the monthly page.
- Add further transit lanes or markers only as explicit product requirements.

## 7. Maintenance Rule

When a backlog item is implemented:

1. Move its requirements into the relevant thematic document.
2. Remove or rewrite the item here.
3. Mark the behavior as **Implemented** only after it is tested.
