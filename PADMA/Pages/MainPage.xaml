<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PADMA"
             x:Class="PADMA.MainPage"
             BackgroundColor="White">

    <Grid RowDefinitions="2*,1*" Padding="0" Margin="0">

        <!-- Календарь (2/3 экрана) -->
        <CollectionView x:Name="CalendarCollection"
                        Grid.Row="0"
                        ItemsSource="{Binding Days}"
                        SelectionMode="Single"
                        SelectedItem="{Binding SelectedDay, Mode=TwoWay}"
                        HorizontalOptions="Fill"
                        VerticalOptions="Fill"
                        BackgroundColor="White">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical"
                                 Span="7"
                                 HorizontalItemSpacing="0"
                                 VerticalItemSpacing="0"/>
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="local:DayItem">
                    <!-- Внешняя рамка ячейки (тонкая сетка календаря) -->
                    <Border Stroke="Black"
                            StrokeThickness="0.5"
                            Padding="0"
                            HorizontalOptions="Fill"
                            VerticalOptions="Fill"
                            HeightRequest="{Binding Source={x:Reference CalendarCollection}, Path=Height, Converter={StaticResource HeightToCellHeightConverter}}">

                        <!-- Фон "сегодня" (мягкий голубой). Для выбранного дня теперь ничего не рисуем -->
                        <Border Background="{Binding IsToday, Converter={StaticResource TodayBackgroundConverter}}"
                                Padding="0"
                                HorizontalOptions="Fill"
                                VerticalOptions="Fill">

                            <!-- Верх: число дня (1/3), Низ: 6 полос (2/3) -->
                            <Grid RowDefinitions="*,2*"
                                  RowSpacing="0"
                                  ColumnSpacing="0"
                                  Padding="0"
                                  HorizontalOptions="Fill"
                                  VerticalOptions="Fill">

                                <!-- Число дня слева-сверху -->
                                <Label Grid.Row="0"
                                       Text="{Binding DayNumber}"
                                       FontAttributes="Bold"
                                       HorizontalOptions="Start"
                                       VerticalOptions="Start"
                                       Margin="2,2,0,0"
                                       TextColor="{Binding IsCurrentMonth, Converter={StaticResource BoolToColorConverter}}"/>

                                <!-- 6 цветных полосок (пока статические цвета) -->
                                <Grid Grid.Row="1"
                                      RowDefinitions="*,*,*,*,*,*"
                                      VerticalOptions="Fill"
                                      HorizontalOptions="Fill"
                                      RowSpacing="1"
                                      Padding="0,2,0,2">
                                    <BoxView Grid.Row="0" BackgroundColor="Red"/>
                                    <BoxView Grid.Row="1" BackgroundColor="Orange"/>
                                    <BoxView Grid.Row="2" BackgroundColor="Yellow"/>
                                    <BoxView Grid.Row="3" BackgroundColor="Green"/>
                                    <BoxView Grid.Row="4" BackgroundColor="Blue"/>
                                    <BoxView Grid.Row="5" BackgroundColor="Purple"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Нижняя зона (1/3 экрана): детали выбранного дня -->
        <Grid Grid.Row="1" BackgroundColor="White" Padding="10">
            <Grid RowDefinitions="*,2*"
                  RowSpacing="0"
                  ColumnSpacing="0">

                <!-- Число выбранного дня, крупнее -->
                <Label Grid.Row="0"
                       Text="{Binding SelectedDay.DayNumber}"
                       FontAttributes="Bold"
                       FontSize="28"
                       HorizontalOptions="Start"
                       VerticalOptions="Start"
                       Margin="4,0,0,6"
                       TextColor="{Binding SelectedDay.IsCurrentMonth, Converter={StaticResource BoolToColorConverter}}"/>

                <!-- Те же 6 полосок, растянутые по ширине нижней области -->
                <Grid Grid.Row="1"
                      RowDefinitions="*,*,*,*,*,*"
                      VerticalOptions="Fill"
                      HorizontalOptions="Fill"
                      RowSpacing="2">
                    <BoxView Grid.Row="0" BackgroundColor="Red"/>
                    <BoxView Grid.Row="1" BackgroundColor="Orange"/>
                    <BoxView Grid.Row="2" BackgroundColor="Yellow"/>
                    <BoxView Grid.Row="3" BackgroundColor="Green"/>
                    <BoxView Grid.Row="4" BackgroundColor="Blue"/>
                    <BoxView Grid.Row="5" BackgroundColor="Purple"/>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</ContentPage>
