<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:inputs="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             x:Class="PADMA.Pages.ColorLookupPage"
             Title="Select color">

    <Grid RowDefinitions="Auto,*,Auto"
          Padding="16"
          RowSpacing="12">

        <inputs:SfColorPicker x:Name="picker"
                          Grid.Row="0"
                          IsInline="True"
                          VerticalOptions="Start"
                          HorizontalOptions="FillAndExpand"
                          ColorMode="Spectrum"
                          IsColorModeSwitcherVisible="False"
                          IsActionButtonsVisible="False"
                          ShowInputArea="True"
                          ShowAlphaSlider="True"
                          ColorChanged="OnColorChanged" />

        <Border Grid.Row="1"
                Stroke="#D0D0D0"
                BackgroundColor="White"
                Padding="8">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="10"/>
            </Border.StrokeShape>

            <Border x:Name="SelectedColorPreview"
                    HorizontalOptions="Fill"
                    VerticalOptions="Fill"
                    BackgroundColor="Transparent">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="8"/>
                </Border.StrokeShape>
            </Border>
        </Border>

        <Button x:Name="SelectButon"
                Grid.Row="2"
                Text="{Binding SelectButon}"
                HeightRequest="44"
                HorizontalOptions="FillAndExpand"
                Clicked="OnSelectClicked"
                BackgroundColor="#2196F3"
                TextColor="White"
                CornerRadius="10"/>
    </Grid>

</ContentPage>