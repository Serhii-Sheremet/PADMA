namespace PADMA.Core.Enums
{
    public enum EAspectFilter
    {
        NONE = 0,

        SUN = (int)EPlanet.SUN,
        MOON = (int)EPlanet.MOON,
        MARS = (int)EPlanet.MARS,
        MERCURY = (int)EPlanet.MERCURY,
        JUPITER = (int)EPlanet.JUPITER,
        VENUS = (int)EPlanet.VENUS,
        SATURN = (int)EPlanet.SATURN,
        RAHU = (int)EPlanet.RAHU,

        ALL = 100
    }
}
